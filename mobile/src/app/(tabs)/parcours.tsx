import { ComingSoon } from '@/components/ComingSoon';

export default function ParcoursScreen() {
  return (
    <ComingSoon
      eyebrow="Mon parcours vers"
      title="Parcours"
      note="La timeline du voyage (6 jalons : inscription → départ en Chine) sera branchée sur le statut dossier_bourses."
    />
  );
}
